version: 2
tunnels:
  frontend:
    addr: 3001
    proto: http
  backend:
    addr: 5001
    proto: http

# ngrok auth จะอ่านบน .env ดังนั้นต้อใช้คำสั่งนี้ก่อน
# export NGROK_AUTHTOKEN=$(grep NGROK_AUTHTOKEN .env | cut -d '=' -f2)
# ngrok start --all